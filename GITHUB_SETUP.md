# Инструкция по экспорту проекта в GitHub

## Подготовка проекта

Проект уже подготовлен к экспорту:
- ✅ Создан `.gitignore`
- ✅ Создан `LICENSE` (MIT)
- ✅ Обновлен `package.json` с правильным именем
- ✅ Создан `README.md` с полной документацией

## Шаги для экспорта в GitHub

### 1. Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите кнопку **"New repository"** (или перейдите по ссылке: https://github.com/new)
3. Заполните форму:
   - **Repository name**: `GidroAtlas` (или `gidroatlas`)
   - **Description**: `Интерактивная платформа мониторинга водных ресурсов Казахстана`
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или license (они уже есть в проекте)
4. Нажмите **"Create repository"**

### 2. Инициализация Git в проекте

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация Git репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: GidroAtlas - платформа мониторинга водных ресурсов Казахстана"
```

### 3. Подключение к удаленному репозиторию

```bash
# Добавление удаленного репозитория (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/GidroAtlas.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/GidroAtlas.git
```

### 4. Отправка кода на GitHub

```bash
# Переименование основной ветки в main (если нужно)
git branch -M main

# Отправка кода
git push -u origin main
```

### 5. Проверка

Откройте ваш репозиторий на GitHub и убедитесь, что все файлы загружены.

## Дополнительные настройки GitHub

### Настройка репозитория

1. Перейдите в **Settings** → **General**
2. В разделе **Features** включите нужные опции:
   - ✅ Issues
   - ✅ Discussions (опционально)
   - ✅ Wiki (опционально)

### Добавление тегов (опционально)

Если хотите создать релиз:

```bash
# Создание тега версии
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

Затем на GitHub:
1. Перейдите в **Releases** → **Draft a new release**
2. Выберите созданный тег
3. Заполните описание релиза
4. Опубликуйте

### Настройка GitHub Pages (опционально)

Для публикации проекта на GitHub Pages:

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите:
   - **Branch**: `main` (или `gh-pages`)
   - **Folder**: `/root` (или создайте папку `docs`)
3. Сохраните

**Важно**: Для GitHub Pages нужно собрать проект:

```bash
npm run build
```

Затем либо:
- Используйте папку `dist` как источник
- Или настройте GitHub Actions для автоматической сборки

## Структура файлов для GitHub

Проект содержит следующие важные файлы:

```
GidroAtlas/
├── .gitignore          # Игнорируемые файлы
├── LICENSE             # MIT лицензия
├── README.md           # Полная документация
├── package.json        # Зависимости и скрипты
├── vite.config.ts      # Конфигурация Vite
├── tsconfig.json       # Конфигурация TypeScript
└── src/                # Исходный код
```

## Что НЕ будет загружено (благодаря .gitignore)

- `node_modules/` - зависимости
- `dist/` - собранные файлы
- `.env` - переменные окружения
- Временные файлы редактора

## Полезные команды Git

```bash
# Проверка статуса
git status

# Просмотр изменений
git diff

# Добавление конкретного файла
git add filename

# Коммит с сообщением
git commit -m "Описание изменений"

# Просмотр истории коммитов
git log

# Обновление с удаленного репозитория
git pull origin main

# Отправка изменений
git push origin main
```

## Troubleshooting

### Ошибка: "remote origin already exists"

```bash
# Удаление старого origin
git remote remove origin

# Добавление нового
git remote add origin https://github.com/YOUR_USERNAME/GidroAtlas.git
```

### Ошибка: "failed to push some refs"

```bash
# Получение изменений с удаленного репозитория
git pull origin main --allow-unrelated-histories

# Затем повторите push
git push origin main
```

### Игнорирование больших файлов

Если случайно добавили большие файлы:

```bash
# Удаление из индекса (но не из файловой системы)
git rm --cached large-file.js

# Добавление в .gitignore
echo "large-file.js" >> .gitignore

# Коммит изменений
git commit -m "Remove large file"
```

## Дополнительные ресурсы

- [GitHub Docs](https://docs.github.com/)
- [Git Handbook](https://guides.github.com/introduction/git-handbook/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

---

**Готово!** Ваш проект теперь на GitHub 🎉

