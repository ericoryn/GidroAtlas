# 🌊 GidroAtlas

![React](https://img.shields.io/badge/React-18.3-blue?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5.8-blue?logo=typescript)
![Vite](https://img.shields.io/badge/Vite-5.4-purple?logo=vite)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-3.4-38bdf8?logo=tailwindcss)
![Leaflet](https://img.shields.io/badge/Leaflet-1.9-green?logo=leaflet)
![License](https://img.shields.io/badge/License-MIT-green)

**Интерактивная платформа мониторинга водных ресурсов Казахстана**

GidroAtlas — это веб-приложение для визуализации, мониторинга и управления данными о водных объектах Казахстана. Платформа помогает специалистам определять приоритеты обследования гидротехнических сооружений на основе их технического состояния и актуальности данных.

---

## 📋 Содержание

- [Описание проекта](#-описание-проекта)
- [Основные функции](#-основные-функции)
- [Технологический стек](#-технологический-стек)
- [Требования к системе](#-требования-к-системе)
- [Быстрый старт](#-быстрый-старт)
- [Структура проекта](#-структура-проекта)
- [Система приоритетов](#-система-приоритетов)
- [Роли пользователей](#-роли-пользователей)
- [Разработка](#-разработка)
- [Деплой](#-деплой)
- [FAQ](#-faq)

---

## 🎯 Описание проекта

GidroAtlas — это MVP платформы для визуализации и управления водными ресурсами Казахстана. Проект разработан для помощи гидрологам, экологам и специалистам по водным ресурсам в принятии решений о приоритетах обследования гидротехнических сооружений.

### Основная цель

Помочь специалистам определять, какие водные объекты требуют срочного обследования и обновления паспортов на основе:
- Технического состояния объекта (категория 1-5)
- Возраста паспорта объекта
- Автоматического расчета приоритета

### Ключевые возможности

- 🗺️ **Интерактивная карта** с визуализацией всех водных объектов Казахстана
- 🎨 **Цветовая маркировка** по техническому состоянию (5 категорий)
- 🔍 **Расширенная фильтрация** по множеству параметров
- 📊 **Система приоритетов** с автоматическим расчетом
- 👥 **Двухуровневая система доступа** (Гость/Эксперт)
- 📄 **Детальная информация** о каждом объекте
- 📈 **Таблица приоритетов** для экспертов

---

## ✨ Основные функции

### 🗺️ Интерактивная карта

- Отображение всех водных объектов на карте Казахстана
- Цветовые маркеры по категориям технического состояния
- Клик по маркеру открывает детальную информацию
- Легенда с описанием категорий
- Счетчик отображаемых объектов

### 🔍 Система фильтрации

Фильтрация по следующим параметрам:
- **Регион** — выбор области Казахстана
- **Тип ресурса** — озеро, канал, водохранилище
- **Тип воды** — пресная, соленая
- **Наличие фауны** — да/нет/неизвестно
- **Категория состояния** — от 1 (лучшее) до 5 (критическое)
- **Дата паспорта** — диапазон дат
- **Поиск по названию** — текстовый поиск

### 📊 Система приоритетов

Автоматический расчет приоритета обследования на основе:
- Технического состояния объекта (категория)
- Возраста паспорта объекта

**Формула расчета:**
```
Приоритет = Категория × 3 + Возраст_паспорта_в_годах
```

**Классификация приоритетов:**
- 🔴 **Высокий** (High): ≥ 15 баллов
- 🟡 **Средний** (Medium): 9-14 баллов
- 🟢 **Низкий** (Low): < 9 баллов

### 👥 Система ролей

#### Гостевой доступ
- Просмотр карты и объектов
- Использование фильтров
- Просмотр базовой информации об объектах

#### Эксперт
- Все возможности гостя +
- Доступ к странице "Приоритеты"
- Просмотр расчета приоритета для каждого объекта
- Возможность открытия паспортов объектов
- Расширенная аналитика

---

## 🛠️ Технологический стек

|        Компонент       |         Технологии             |
|------------------------|--------------------------------|
| **Frontend Framework** | React 18.3, TypeScript 5.8     |
| **Build Tool**         | Vite 5.4                       |
| **Стилизация**         | Tailwind CSS 3.4, shadcn/ui    |
| **Карты**              | Leaflet 1.9, React-Leaflet 4.2 |
| **Роутинг**            | React Router DOM 6.30          |
| **State Management**   | Zustand 4.5                    |
| **HTTP Client**        | Axios 1.13, TanStack Query 5.8 |
| **UI Components**      | Radix UI, Lucide React         |
| **Формы**              | React Hook Form 7.6, Zod 3.25  |
| **Графики**            | Recharts 2.15                  |
| **Иконки**             | Lucide React 0.462             |

### Ключевые библиотеки

- **Zustand** — легковесное управление состоянием
- **React-Leaflet** — интеграция карт Leaflet в React
- **shadcn/ui** — набор переиспользуемых UI компонентов
- **TanStack Query** — управление серверным состоянием и кэшированием
- **Zod** — валидация схем данных

---

## 💻 Требования к системе

### Минимальные требования

- Node.js: 18.0 или выше
- npm: 9.0 или выше (или yarn/pnpm)
- Оперативная память: 4GB RAM
- Браузер: современный браузер с поддержкой ES2020+
  - Chrome/Edge 90+
  - Firefox 88+
  - Safari 14+

### Рекомендуемые требования

- **Node.js**: 20.0 или выше
- **Оперативная память**: 8GB RAM
- **Дисковое пространство**: 500MB для node_modules

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/ericoryn/gidroatlas.git
cd gidroatlas
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:8080`

### 4. Сборка для production

```bash
npm run build
```

### 5. Просмотр production сборки

```bash
npm run preview
```

---

## 📁 Структура проекта

```
gidroatlas/
├── public/                 # Статические файлы
├── src/
│   ├── components/        # React компоненты
│   │   ├── details/       # Компоненты деталей объектов
│   │   ├── filters/       # Компоненты фильтрации
│   │   ├── layout/        # Компоненты макета (Header)
│   │   ├── map/           # Компоненты карты
│   │   └── ui/            # UI компоненты (shadcn/ui)
│   ├── data/              # Моковые данные
│   │   └── mockData.ts    # Данные водных объектов
│   ├── hooks/             # Кастомные React хуки
│   ├── lib/               # Утилиты и хелперы
│   │   └── utils.ts       # Общие утилиты
│   ├── pages/             # Страницы приложения
│   │   ├── Index.tsx      # Главная страница с картой
│   │   ├── Login.tsx      # Страница входа
│   │   ├── Dashboard.tsx  # Страница приоритетов (эксперты)
│   │   └── NotFound.tsx   # 404 страница
│   ├── store/             # Управление состоянием
│   │   └── useStore.ts    # Zustand store
│   ├── types/             # TypeScript типы
│   │   └── index.ts       # Определения типов
│   ├── App.tsx            # Корневой компонент
│   ├── main.tsx           # Точка входа
│   └── index.css          # Глобальные стили
├── .eslintrc.js           # Конфигурация ESLint
├── components.json        # Конфигурация shadcn/ui
├── index.html            # HTML шаблон
├── package.json          # Зависимости проекта
├── postcss.config.js     # Конфигурация PostCSS
├── tailwind.config.ts    # Конфигурация Tailwind
├── tsconfig.json         # Конфигурация TypeScript
├── vite.config.ts        # Конфигурация Vite
└── README.md             # Документация
```

### Ключевые файлы

- **`src/store/useStore.ts`** — централизованное управление состоянием (Zustand)
- **`src/data/mockData.ts`** — моковые данные водных объектов
- **`src/types/index.ts`** — TypeScript типы и интерфейсы
- **`src/components/map/WaterMap.tsx`** — компонент карты с Leaflet
- **`src/components/filters/FilterPanel.tsx`** — панель фильтров
- **`src/pages/Dashboard.tsx`** — страница приоритетов для экспертов

---

## 📊 Система приоритетов

### Алгоритм расчета

Приоритет обследования рассчитывается автоматически для каждого объекта на основе двух факторов:

1. **Техническое состояние** (категория 1-5)
2. **Возраст паспорта** (количество лет с момента последнего обследования)

### Формула

```typescript
Приоритет = Категория × 3 + Возраст_паспорта_в_годах
```

### Логика

- **Чем хуже состояние** (выше номер категории), **тем выше приоритет**
- Категория 1 (лучшее состояние) → низкий приоритет
- Категория 5 (критическое состояние) → высокий приоритет
- Старые паспорта увеличивают приоритет

### Примеры расчета

|      Объект        |    Категория    | Возраст паспорта  | Балл | Приоритет |
|--------------------|-----------------|-------------------|------|-----------|
| Озеро Зайсан       | 1 (лучшее)      | 1 год             | 4    |  Низкий   |
| Озеро Балхаш       | 2 (хорошее)     | 3 года            | 9    |  Средний  |
| Капчагайское       | 3 (среднее)     | 4 года            | 13   |  Средний  |
| Иртыш-Караганда    | 4 (плохое)      | 6 лет             | 18   |  Высокий  |
| Шардаринское       | 5 (критическое) | 8 лет             | 23   |  Высокий  |

### Категории состояния

| Категория |      Цвет          |     Описание                | Мин. балл |
|     1     |  🟢 Зеленый       | Отличное состояние           |     3     |
|     2     | 🟢 Светло-зеленый | Хорошее состояние            |     6     |
|     3     | 🟡 Желтый         | Удовлетворительное состояние |     9     |
|     4     | 🟠 Оранжевый      | Плохое состояние             |     12    |
|     5     | 🔴 Красный        | Критическое состояние        |     15    |

---

## 👥 Роли пользователей

### Гостевой доступ

**Возможности:**
- ✅ Просмотр интерактивной карты
- ✅ Использование всех фильтров
- ✅ Просмотр базовой информации об объектах
- ✅ Поиск объектов по названию

**Ограничения:**
- ❌ Нет доступа к странице "Приоритеты"
- ❌ Не видит расчет приоритета
- ❌ Не может открывать паспорта объектов

### Эксперт

**Все возможности гостя +**
- ✅ Доступ к странице "Приоритеты" (`/dashboard`)
- ✅ Просмотр таблицы всех объектов с приоритетами
- ✅ Сортировка по приоритету, категории, возрасту паспорта
- ✅ Просмотр расчета приоритета для каждого объекта
- ✅ Кнопка "Открыть паспорт" в деталях объекта
- ✅ Расширенная аналитика

### Тестовые учетные записи

Для входа в систему используйте следующие учетные данные:

| Роль | Логин | Пароль |
|------|-------|--------|
| Эксперт | `expert` | `expert123` |
| Админ | `admin` | `admin123` |

> **Примечание:** В текущей версии админ имеет те же права, что и эксперт.

---

## 🛠️ Разработка

### Настройка окружения разработки

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/ericoryn/gidroatlas.git
cd gidroatlas
```

2. **Установите зависимости**
```bash
npm install
```

3. **Запустите dev сервер**
```bash
npm run dev
```

### Работа с данными

#### Добавление новых объектов

Отредактируйте файл `src/data/mockData.ts`:

```typescript
export const mockWaterObjects: WaterObject[] = [
  {
    id: 'unique-id',
    name: 'Название объекта',
    region: 'Название области',
    resourceType: 'lake' | 'canal' | 'reservoir',
    waterType: 'fresh' | 'non-fresh',
    fauna: 'yes' | 'no' | 'unknown',
    conditionCategory: 1 | 2 | 3 | 4 | 5,
    passportDate: 'YYYY-MM-DD',
    latitude: 0.0,
    longitude: 0.0,
    description: 'Описание объекта',
    area: 0, // опционально
    depth: 0, // опционально
  },
  // ...
];
```

#### Изменение формулы приоритета

Формула находится в `src/store/useStore.ts`:

```typescript
export function calculatePriority(object: WaterObject) {
  const ageInYears = /* расчет возраста */;
  const score = object.conditionCategory * 3 + ageInYears;
  // ...
}
```

### Добавление новых фильтров

1. Добавьте поле в `FilterParams` (`src/types/index.ts`)
2. Обновите `defaultFilters` в `src/store/useStore.ts`
3. Добавьте UI компонент в `src/components/filters/FilterPanel.tsx`
4. Обновите функцию фильтрации в `src/pages/Index.tsx`

### Линтинг и форматирование

```bash
# Проверка кода
npm run lint

# Автоисправление (если настроено)
npm run lint -- --fix
```

### Структура компонентов

Компоненты организованы по функциональности:
- `components/details/` — детали объектов
- `components/filters/` — фильтры
- `components/layout/` — макет страницы
- `components/map/` — карта
- `components/ui/` — переиспользуемые UI компоненты

---

## 🚀 Деплой

### Production сборка

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

### Деплой на Vercel

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Деплой:
```bash
vercel
```

### Деплой на Netlify

1. Подключите репозиторий к Netlify
2. Настройки сборки:
   - Build command: `npm run build`
   - Publish directory: `dist`

### Деплой на статический хостинг

1. Соберите проект:
```bash
npm run build
```

2. Загрузите содержимое папки `dist/` на ваш хостинг

### Docker (опционально)

Если нужен Docker:

```dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

---

## 📝 Скрипты и команды

### Доступные команды

|      Команда        |               Описание                 |
|---------------------|----------------------------------------|
| `npm run dev`       | Запуск dev сервера на `localhost:8080` |
| `npm run build`     | Production сборка                      |
| `npm run build:dev` | Development сборка                     |
| `npm run preview`   | Просмотр production сборки             |
| `npm run lint`      | Проверка кода ESLint                   |

### Переменные окружения

В текущей версии переменные окружения не используются. Все настройки находятся в коде.

---

## ❓ FAQ

### Как добавить новый регион?

Отредактируйте массив `uniqueRegions` в `src/components/filters/FilterPanel.tsx` или добавьте его в данные объектов в `src/data/mockData.ts`.

### Как изменить пороги приоритетов?

Отредактируйте функцию `calculatePriority` в `src/store/useStore.ts`:

```typescript
if (score >= 15) {
  level = 'high';
} else if (score >= 9) {
  level = 'medium';
} else {
  level = 'low';
}
```

### Почему карта не загружается?

Проверьте:
1. Интернет-соединение (карта использует OpenStreetMap)
2. Консоль браузера на наличие ошибок
3. Что Leaflet CSS подключен в `index.html`

### Как добавить новый тип водного объекта?

1. Добавьте тип в `WaterResourceType` (`src/types/index.ts`)
2. Добавьте метку в `RESOURCE_TYPE_LABELS`
3. Обновите данные в `mockData.ts`

### Как работает авторизация?

В текущей версии используется моковая авторизация. Данные хранятся в `src/store/useStore.ts` в массиве `MOCK_USERS`. Для production необходимо подключить реальный backend.

---

## 🗺️ Roadmap

### Планируемые функции

- [ ] Интеграция с реальным backend API
- [ ] Экспорт данных в Excel/PDF
- [ ] Расширенная аналитика и графики
- [ ] История изменений объектов
- [ ] Уведомления о критических объектах
- [ ] Мобильная версия
- [ ] Темная тема (dark mode)
- [ ] Мультиязычность (казахский, английский)
- [ ] Интеграция с GIS системами
- [ ] API для сторонних приложений

### Улучшения

- [ ] Оптимизация производительности карты
- [ ] Кэширование данных
- [ ] Offline режим
- [ ] PWA поддержка
- [ ] Unit тесты
- [ ] E2E тесты

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

---

## 👨‍💻 Команда проекта

Проект разработан в рамках хакатона для решения задач мониторинга водных ресурсов Казахстана.

---

## 🙏 Благодарности

- **Leaflet** — отличная библиотека для интерактивных карт
- **shadcn/ui** — набор качественных UI компонентов
- **React** — мощный фреймворк для создания пользовательских интерфейсов
- **Vite** — быстрый инструмент сборки
- **OpenStreetMap** — открытые картографические данные

---

## 📞 Обратная связь

Если у вас есть вопросы, предложения или вы нашли ошибку, пожалуйста:
- Создайте Issue в репозитории
- Напишите на email: asterprio@gmail.com
- Присоединитесь к обсуждению в Discussions

---

**Сделано с ❤️ для водных ресурсов Казахстана**
