# 🚀 Быстрый экспорт в GitHub

## Команды для копирования (выполняйте по порядку)

```bash
# 1. Инициализация Git
git init

# 2. Добавление всех файлов
git add .

# 3. Первый коммит
git commit -m "Initial commit: GidroAtlas - платформа мониторинга водных ресурсов Казахстана"

# 4. Переименование ветки в main
git branch -M main

# 5. Добавление удаленного репозитория (замените YOUR_USERNAME)
git remote add origin https://github.com/ericoryn/GidroAtlas.git

# 6. Отправка кода на GitHub
git push -u origin main
```

## Перед выполнением команд

1. ✅ Создайте репозиторий на GitHub (без README, .gitignore, license)
2. ✅ Замените `YOUR_USERNAME` на ваш GitHub username в команде №5
3. ✅ Убедитесь, что вы авторизованы в Git:
   ```bash
   git config --global user.name "Ваше Имя"
   git config --global user.email "your.email@example.com"
   ```

## После экспорта

- ✅ Проверьте, что все файлы загружены на GitHub
- ✅ Убедитесь, что README.md отображается корректно
- ✅ Проверьте, что `node_modules` не загружен (благодаря .gitignore)

## Если что-то пошло не так

См. подробную инструкцию в файле `GITHUB_SETUP.md`

